<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bindingdata">
        <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BindingData">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PractiseView">

        </activity>
        <activity android:name=".recycler_view_example.RecyclerViewExample">

        </activity>
        <activity android:name=".user_details.UserDetailsActivity"
            />
<activity android:name=".TimePickerExample"/>
<activity android:name=".DatePickerExample"/>

    </application>

</manifest>